import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  app: {
    margin: 16
  },
  header: {
    fontSize: 32,
    marginVertical: 8
  },
  newNote: {
    alignItems: 'center',
    padding: 10,
    borderWidth: 1,
    borderColor: '#000',
    borderRadius: 4
  }
});

export default styles;
